export function createProxyRuntime() {
  return {
    ready: false,
    provider: 'planned',
    mountPath: '/service/proxy',
    message: 'Proxy runtime adapter placeholder. Replace this with the real server-side integration during Interstellar-compatible wiring.'
  };
}
