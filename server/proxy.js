export function proxyStatus() {
  return {
    ready: false,
    mode: 'planned',
    message: 'Proxy backend integration is not wired yet. This lane is reserved for the real server-side runtime.'
  };
}
